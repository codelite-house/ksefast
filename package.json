{
  "name": "ksefast",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "start": "npm --workspace backend run start",
    "deploy": "vercel"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
