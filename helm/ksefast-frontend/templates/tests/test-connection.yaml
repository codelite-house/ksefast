apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ksefast-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "ksefast-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ksefast-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
